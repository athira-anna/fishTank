services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fishtank
      POSTGRES_PASSWORD: fishtank
      POSTGRES_DB: fishtank
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
